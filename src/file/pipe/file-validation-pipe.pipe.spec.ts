import { FileValidationPipePipe } from './file-validation-pipe.pipe';

describe('FileValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new FileValidationPipePipe()).toBeDefined();
  });
});
